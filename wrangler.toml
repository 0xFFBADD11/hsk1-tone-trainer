name = "hsk1-tone-trainer"
main = "worker/index.js"
compatibility_date = "2025-01-01"

# The worker runs ahead of the static asset handler so its security headers
# are applied to every response, including the static files themselves.
[assets]
directory = "."
binding = "ASSETS"
run_worker_first = true
